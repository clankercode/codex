- [x] allow injecting non-user messages (assistant, system, etc)
- [] stdout format and details (for thin)
- [x] support replacing system prompt later
- [] tool call passthrough/externally registered (external MCP passthrough too?)
- [x] compact statusline for ctx etc
- [x] add easy single CLI flag to disable as much built-in context as possible. see this comment from ~/src/codename-thin implementing a wrapper around codex:

• [...] but Codex exposes them mostly as -c config overrides rather than dedicated CLI
  flags.

  Implemented now:

  - features.shell_tool=false
  - features.apps=false
  - features.plugins=false
  - features.tool_search=false
  - features.tool_suggest=false
  - features.image_generation=false
  - features.multi_agent=false
  - features.memories=false
  - features.skill_mcp_dependency_install=false
  - skills.bundled.enabled=false
  - project_doc_max_bytes=0
  - include_apps_instructions=false
  - include_permissions_instructions=false
  - Cursor: --mode ask
  - Cursor: new extra_args passthrough for any local CLI-specific knobs

  Other knobs I found but did not implement because they either do not reduce prompt/context
  much or are situational:

  - hide_agent_reasoning=true: output/UI behavior, not really prompt/context reduction.
  - model_context_window: metadata/limits, not a context reducer.
  - model_auto_compact_token_limit: can reduce long-session growth, but it changes conversation
    behavior and is not a first-turn savings knob.
  - tool_output_token_limit: only matters after tool calls, and we’re disabling native tools by
    default.
  - web_search = ...: largely redundant with disabling app/search/tool features for this use.

  One thing worth revisiting in the Codex fork: a first-class “text provider mode” or “disable
  all model-visible tools” config would be better than a bundle of feature overrides. I didn’t
  see one clean current flag, so Thin now applies the explicit lean set.


- [x] codename-thin coder agent wishlist. 

Please see this response from the coder agent about the wishlist. 
All paths are relative to ~/src/codename-thin/

--- start ---

Yes. The original wishlist you asked for is here:

  - docs/superpowers/plans/2026-04-17-codex-cli-persistent-provider.md:578

  That section includes 7 items:

  1. codex-turn-start-bridge --output-format jsonl
  2. Stable public Python/TypeScript app-server SDK
  3. thread/injectMessages style typed message append API
  4. Turn-scoped base-instructions update
  5. First-class no-tool/no-shell mode
  6. Structured usage/quota notifications
  7. Lossless thread/read raw event replay

  There’s also a later follow-up wishlist here:

  - docs/codex-cli-adversarial-findings.md:146

  That one adds more concrete fork-facing asks:

  - capability introspection JSON
  - stable error codes
  - explicit timeout/cancellation semantics
  - thread replay/import API
  - event and usage capture contract
  - SDK keyword stability

  If you want the most actionable shortlist to hand to the Codex fork work, I’d
  prioritize:

  1. capability introspection
  2. stable error codes
  3. event/usage capture contract
  4. thread replay/import API
  5. first-class no-tool/no-shell mode

--- end ---

For this, assemble features into a matrix as columns with the rows as goals:
- suitable for a codex fork
- reasonable workload (not massive features)
- low maintenance: can be implemented with minimal/reasonable touching of other code

and list any other suitable goals you can think of. 
for each feature, we only want to implement it now if we can meet all goals. 
if it fails any goal, defer it till later.
Insert the matrix in this file below. 

** INSERT FEATURE MATRIX HERE **

| Goal / Feature | typed message append API | turn-scoped base instructions | first-class no-tool/no-shell mode | capability introspection JSON | stable error codes | event and usage capture contract | structured usage/quota notifications | lossless raw event replay |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| suitable for a codex fork | yes | yes | yes | yes | yes | yes | yes | yes |
| reasonable workload (not massive) | yes | yes | yes | yes | yes | no | yes | no |
| low maintenance / limited code touch | yes | yes | yes | partial | yes | no | yes | no |
| directly useful to thin right now | yes | yes | yes | yes | yes | partial | yes | partial |
| builds on existing substrate in this repo | yes | yes | yes | partial | yes | partial | yes | partial |
| clear downstream API value | yes | yes | yes | yes | yes | yes | yes | yes |
| current repo already has meaningful substrate | yes | yes | yes | partial | yes | partial | yes | partial |
| decision | implement now | implement now | implement now | defer | defer new work | defer | defer new work | defer |

Notes:
- `stable error codes` and `structured usage/quota notifications` already have meaningful support in the fork, so they do not need net-new code in this pass.
- `capability introspection JSON` looks plausible but still wants a cleaner contract than the current mix of feature/config endpoints.
- `event and usage capture contract` and `lossless raw event replay` both cross enough boundaries that they miss the "reasonable workload" and "low maintenance" gates for a quick fork pass.

---

- [x] our build of codex reports v0.0.0 which causes some problems. remove the special case for this in the test and update it to use the most recent actual version number in git history. 

Sample demonstrating problem, starting codex:

codename-thin master  ✗ codex
╭──────────────────────────────────────────────╮
│ >_ OpenAI Codex (v0.0.0)                     │
│                                              │
│ model:     gpt-5.4 medium   /model to change │
│ directory: ~/src/codename-thin               │
╰──────────────────────────────────────────────╯

  Tip: Update Required - This version will no longer be supported starting May 8th. Please upgrade to the latest ve
  rsion (https://github.com/openai/codex/releases/latest) using your preferred package manager.

---

- [x] support compacting using a lighter model. eg if we have been afk so LLM cache is cold, it is advantageous to compact using eg gpt-5.4-mini and then swap the model back to what it was so we can continue with lesser burden on state.
- [x] replicate ~/src/claude-inject-idle-time features, injection should be able to be enabled or disabled live (default on). also add status line option for idle time since last model turn. 
- [x] add `/effort [off|low|medium|high|xhigh]` command which can be used live (and during assistant's turn) to set the effort-level / thinking-level for the current session and model calls (should update immediately).

---

- [x] Idle statusline element should, after user sends a message, change to showing the agent running time (ie time since last user message) while the agent is working (so it shows the complete runtime since last user's msg). It should also show the time since the last user steering message (which I guess technically involves a new turn, but the important distinction was that the agent was not idle at that time). It should show 3 things:
  1. duration of current step.
  2. (optional) time since last user steer (does not show up if user has not steered; state flag resets when idle)
  3. time since last user message at idle

user message at idle are normal prompts. agent finishes work, we're idle, then user sends message. 

steering messages (that's what codex calls them) are user messages sent while the agent is active and they might count as a turn (technically i think yes but codex might not call them that, you'll need to check). 

---

### improvements to idle/timing messages in log

see codex log for annotated example output

- [x] fix: the `[after Xm Ys]` -- it should not show up for steering messages that interrupt the agent (and if they were to show it should be [after 0s] because the agent was not idle).
- [x] when an agent finishes, we should add a message like: "[HH:MM | Δt 1h 2m 14s]", this can be our turn-timings row. It shows: the current time at finish and the duration that the agnets turn made (time since last user message). Then, while we're idle, we can update it every second with a suffixed " | Idle for Xm Ys". (Note: this should only be a visual update, no on-disk changes or whatever). We stop updating it when the user sends a message.

<code-log>

<label> last msg from codex before waiting on bg script </label>
• It is 09:40:48 AEST; Gemini reported reset around 10:19 AEST. There’s no active patent process to wait
  on right now, so I’m waiting until just after the reset window and then I’ll relaunch the E2E run into
  the same tmp.gemini31pro-2-pat.txt transcript.

<label> then i interrupt </label>
• Model interrupted to submit steer instructions.

› Update instructions in .goal-loops/active-goal.md and AGENTS.md: goal-loop iterations should be tied to
  patent generation run. Do not advance the iteration without first waiting for runners to return so you
  can evaluate final results.
  After that, continue waiting for the bg terminal

<label> auto added when I sent </label>
• [after 49m 23s]

</codex-log>

---

- [x] when I run `/effort abcd`, it works, but the command should be cleared from the chat input. 
- [x] investigate and implement if practical: `/permissions` should work when the agent is in-progress. (Currently it fails with an error)
- [x] `[17:00 | Δt 1m 31s | Idle for 0s]` is good, but the idle timer never updates (and it should; otherwise if its too expensive to do all the time, we should omit it and ideally only overwrite once when the user chats next. If that isn't possible, then remove it and we should add it back after the user sends their message -- oh and is it possible for it to be more compact? It currently takes up 2 lines after the user message, but it'd be better if it took up only 1.) Oh, and please update `[17:00 | Δt 1m 31s` to be like `[Agent finish @ HH:MM | Δt 1m 31s`, and have the HH:MM and Xm Ys parts be highlighted, like codeblocks in markdown or that sort of thing. 
- [x] 'Steer' in statusline counts from when the user pressed enter, but it should probably count from when the steering was injected
- [x] The statusline can show you going idle even when the agent is working. Somehow it's possible to trigger it to change from running to idle at some point without the agent actually stopping. might be related to waiting for subagents or something. 
- [x] The first message of a conversation should not get an 'After X' message after it. 
